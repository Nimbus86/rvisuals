---
import "../styles/global.css";
const { title = "rvisuals" } = Astro.props;
---
<!DOCTYPE html>
<html lang="nl">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <link rel="stylesheet" href="/styles/global.css" />
    
  </head>
  <body>
    <header class="site-header">
      <nav class="nav">
        <!-- Linkerkant: icoon + naam -->
        <div class="nav-left">
          <span class="letter">R</span>
          <span class="logo-text">VISUALS</span>
        </div>

        <!-- Rechterkant: menuknoppen -->
        <ul class="nav-links">
          <li><a href={`${import.meta.env.BASE_URL}index`}>Home</a></li>
          <li><a href={`${import.meta.env.BASE_URL}projects`}>Projects</a></li>
          <li><a href={`${import.meta.env.BASE_URL}about`}>About</a></li>
          <li><a href={`${import.meta.env.BASE_URL}contact`}>Contact</a></li>
        </ul>
      </nav>
    </header>

    <main>
   
    
      <slot />
    </main>

    <footer class="site-footer">
      <p>© {new Date().getFullYear()} rvisuals</p> 
    </footer>
  </body>
</html>

